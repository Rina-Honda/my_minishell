１．'make -n'＋'-Wall -Wextra -Werror'が使用されていますか？

２．Makefileは再リンクしてませんか？

３．シンプルなコマンドとグローバル変数を試す
	→/bin/lsのような絶対パスを指定した単純コマンドorオプションなしのその他のコマンド

４．グローバル変数はいくつ？

５．グローバル変数は受信した信号の数以外の情報やデータアクセスはNG

６．空コマンドの実行

７．スペースまたはタブのみの実行

８．/bin/ls のような絶対パスを持つ単純なコマンド、または引用符や二重引用符なしで引数を持つその他のコマンドを実行

９．異なるコマンドと引数を使用して複数回繰り返す

１０．引数の有無に関わらず、-nオプションを指定してechoコマンドを実行

１１．引数の有無に関わらずexitコマンドを実行
		→異なる引数で複数回繰り返す

１２．/bin/ls のような絶対パスを持つ単純なコマンド、または引用符や二重引用符なしで引数を持つその他のコマンドを実行します。
		→次に echo $? を実行

１３．異なるコマンドと引数で複数回繰り返す
		→例）/bin/ls filethatdoesntexist
				expr $? + $?

１４．空のプロンプトでCtrl+C →新しいプロンプトを含む行が表示
		 空のプロンプトでCtrl-\ →何も起こらない
		 空のプロンプトでCtrl+D →ミニシェルが終了
		(何かを書いた後のプロンプトで Ctrl + C を押すと、新しい行と新しいプロンプトが表示されます。
		バッファもクリーンである必要があります。前の行から何も実行されていないことを確認するには、「Enter」を押します。)

１５．引数なしの cat や grep "something" などのブロッキング コマンド→Ctrl+C

１６．引数なしの cat や grep "something" などのブロッキング コマンド→Ctrl-\

１７．引数なしの cat や grep "something" などのブロッキング コマンド→→Ctrl+D

１８．echo "cat lol.c | cat > lol.c"

１９．echo '$USER' は "$USER" を出力する必要がある

２０．envで今の環境変数が表示されうかどうか

２１．環境変数をエクスポートし、新しい環境変数を作成して古い環境変数を置き換えて、反映されているか確認

２２．
